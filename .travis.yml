language: bash

notifications:
  slack:
    rooms:
      - mapic-io:NKaii0z3tRkeDyXaDbRauRFp

dist: trusty
sudo: required

install:
  - cd cli
  - sudo bash mapic-cli.sh
  - mapic version
  - mapic domain localhost
  - mapic config set MAPIC_EMAIL travis@mapic.io
  - mapic ssl create

script:
  - sudo mapic travis start
